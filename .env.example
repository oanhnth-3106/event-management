# =====================================================================
# Environment Variables Template
# =====================================================================
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to version control
# =====================================================================

# ---------------------------------------------------------------------
# Supabase Configuration
# ---------------------------------------------------------------------
# Get these from: https://app.supabase.com/project/_/settings/api

# Public variables (safe to expose to client)
NEXT_PUBLIC_SUPABASE_URL=your-project-url.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Secret variables (NEVER expose to client)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ---------------------------------------------------------------------
# QR Code Security
# ---------------------------------------------------------------------
# Secret key for HMAC-SHA256 signature generation
# Generate with: openssl rand -hex 32
QR_SECRET_KEY=your-random-secret-key-here

# ---------------------------------------------------------------------
# Email Service (Required for notifications & reminders)
# ---------------------------------------------------------------------
# Email provider: resend (recommended), sendgrid, or nodemailer
EMAIL_PROVIDER=resend

# From email configuration
EMAIL_FROM=noreply@yourdomain.com
EMAIL_FROM_NAME=EventHub
EMAIL_SUPPORT=support@yourdomain.com

# --- RESEND (Recommended) ---
# Get API key from: https://resend.com/api-keys
RESEND_API_KEY=re_your_api_key_here

# --- SENDGRID (Alternative) ---
# Get API key from: https://app.sendgrid.com/settings/api_keys
# SENDGRID_API_KEY=SG.your_api_key_here

# --- NODEMAILER / SMTP (Alternative) ---
# For Gmail: Use App Password (https://myaccount.google.com/apppasswords)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your@email.com
# SMTP_PASS=your-app-password

# ---------------------------------------------------------------------
# Application Configuration
# ---------------------------------------------------------------------
# Base URL for your application
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Node environment
NODE_ENV=development

# ---------------------------------------------------------------------
# Vercel Deployment (Optional)
# ---------------------------------------------------------------------
# These are automatically set by Vercel
# VERCEL_URL=your-app.vercel.app
# VERCEL_ENV=production

# Cron job secret (for protecting cron endpoints)
# Generate with: openssl rand -hex 32
CRON_SECRET=your-cron-secret-here
